/* Button.jsx */

/*
function Button({
  variant = "default",
  size = "1",
  layout = "normal",
  children
}) {
  return (
    <button
      className={`btn btn--${variant} btn--size-${size} btn--type-${layout}`}
    >
      {children}
    </button>
  )
}

export default Button

<Button variant="secondary" size="2" layout="textarea">
    
</Button>

*/