/* Button.jsx */

/*
function Input({
  variant = "default",
  size = "1",
  layout = "text",
  children
}) {
  return (
    <input
      className={`input input--${variant} input--size-${size} input--type-${layout}`}
    >
  )
}

export default Input

<Input variant="secondary" size="2" layout="textarea">

*/