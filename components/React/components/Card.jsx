/* Card.jsx */

/*
function Card({
  variant = "default",
  size = "1",
  layout = "vertical",
  backgroundImage = "src/assets/image/img-01.jpeg",
  children
}) {
  return (
    <article className="card__container card--size-${size} card--type-${layout}">
        <div className="card card--${variant}" style=`background-image: ${backgroundImage}`}>
            <div className="card__meta">
                <h4>Neón City</h4>
                <p>Una serie sobre detectives en una megaciudad futurista.</p>
            </div>
        </div>
    </article>
  )
}

export default Card

<Card variant="default" size="1" layout="vertical" backgroundImage="src/assets/image/img-01.jpeg">
    children
</Card>

*/